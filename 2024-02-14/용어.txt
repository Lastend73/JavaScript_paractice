1. <srcipt></srcipt>
2. CDN :
3. bootStrap은 4는 jquery 의존(dependency) 한다

4. 자바스크립트는 웹브라우저가 실행
    따라서 브라우저마다 다르게 동작할수 있다 -> jquery 사용하면 신경안써도 됨

5. type - 타입은 값의 따라 자바스크립트가 판단합니다.
    undefiend - 알 수 없는 타입(기본타임)
    number
    String
    boolean
    object

6. undefiend : 알수 없는 값 타입
   null : 참조변수가 아직 가리키는 대상이 없다(typeof로 찍으면 object)

7. scope : 변수를 사용할 수 있는 범위
    python의 스코프는 함수, 자바의 스코프는 중괄호
    var로 변수를 만들면 함수 (ES5)
    let, const를 사용하면 중괄호 (ES6)

8.  JS에서 변수가 올 수 있는 곳에는 함수도 될수 있다
    js는 이름없는 함수를 만들수 있다 (익명 함수)

9. callback 함수 : 위탁실행하는 함수 